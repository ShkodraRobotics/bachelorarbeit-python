import app_class_tk as at

import tester as t
class slid_me:
    def __init__(self):
        pass
    def slider0 (i):
        ls0 = "A" + i + ";"


        #t.sep.seper(ls0)

        at.nootebook_frame.giver(ls0)
    def slider1(i):
        ls0 = "B" + i
        at.nootebook_frame.giver(ls0)
    def slider2(i):
        ls0 = "C" + i
        at.nootebook_frame.giver(ls0)
    def slider3(i):
        ls0 = "D" + i
        at.nootebook_frame.giver(ls0)
    def slider4(i):
        ls0 = "E" + i
        at.nootebook_frame.giver(ls0)

